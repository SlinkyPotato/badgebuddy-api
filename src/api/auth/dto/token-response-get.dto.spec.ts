import { TokenResponsePostDto } from './get-token-response.dto';

describe('GetTokenResponseDto', () => {
  it('should be defined', () => {
    expect(new TokenResponsePostDto()).toBeDefined();
  });
});
