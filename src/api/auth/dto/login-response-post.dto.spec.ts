import { LoginPostResponseDto } from './login-post-response.dto';

describe('LoginResponsePostDto', () => {
  it('should be defined', () => {
    expect(new LoginPostResponseDto()).toBeDefined();
  });
});
