import { VerifyPatchRequestDto } from './verify-patch-request.dto';

describe('VerifyPatchRequestDto', () => {
  it('should be defined', () => {
    expect(new VerifyPatchRequestDto()).toBeDefined();
  });
});
