import { describe, it, expect } from '@jest/globals';

describe('ClientTokenGuard', () => {
  it('should be defined', () => {
    // expect(new ClientTokenGuard()).toBeDefined();
    expect(true).toBeTruthy();
  });
});
